{
  "name": "nc500-visualise",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "data": "tsx scripts/build-data.ts",
    "scrub": "tsx scripts/scrub-gpx.ts",
    "dev": "npm run data && vite",
    "build": "npm run data && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "maplibre-gl": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "fast-xml-parser": "^5.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
